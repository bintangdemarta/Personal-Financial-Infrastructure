# Automation Guide

## Rules

- Create rules for recurring transactions.
- Use tags for infrastructure and project costs.

## Cron

- Ensure cron sidecar runs daily (see `docker-compose.yml`).
- Verify with `docker compose logs cron`.
