# Monitoring Setup

## Basic

- Run health checks: `bash scripts/healthcheck.sh`.
- Review logs: `docker compose logs -f`.

## Optional

- Use Prometheus/Grafana for host metrics.
- Configure alerting via email or a webhook.
